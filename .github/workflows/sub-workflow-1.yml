name: "My sub workflow"

on:
  workflow_call:
    inputs:
      project:
        required: true
        type: string
      project-root:
        required: true
        type: string
      os:
        required: true
        type: string
jobs:
  reusable_workflow_job:
    runs-on: ubuntu-latest
    steps:
      - name: step1
        run: |
          set -x
          echo "i'm running with ${{ inputs.project }} and ${{ inputs.project-root }}"
